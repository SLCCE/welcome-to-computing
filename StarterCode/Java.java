public class Java {
  public static void main(String[] args) {
    // Create Scanner for user input (Replace the question mark below)
    ? in = new Scanner(System.in);

    // Ask for favorite animal (Replace the question marks below)
    System.out.print(?);
    ? favAnimal = ?;

    // Ask for favorite number (Replace the question marks below)
    System.out.print(?);
    ? favNumber = ?;

    // Print message adding context to these variables (Replace the question mark below with your function)
    ?
  }
}
